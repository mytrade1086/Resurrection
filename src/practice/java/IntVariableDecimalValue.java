/*
 * package practice.java;
 * 
 * class IntVariableDecimalValue { public static void main(String args[]) { int
 * a=123.456; // We have assigned a decimal value to a variable which is
 * declared as int System.out.println(a); } }
 * 
 * //Output of this program:
 * 
 * //Error displayed while compiling the program.
 * "Required int but found double"
 * 
 * //This means that Java is telling us to provide the integer value instead of
 * double (i.e. decimal) value.
 */